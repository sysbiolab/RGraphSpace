% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gspace-accessors.R
\name{gs_compute}
\alias{gs_compute}
\title{Apply an igraph function to the graph inside a GraphSpace}
\usage{
gs_compute(gs, .f, ...)
}
\arguments{
\item{gs}{A \code{GraphSpace} object.}

\item{.f}{An \pkg{igraph} function, or the name of one as a string.}

\item{...}{Further arguments passed on to \code{.f}.}
}
\value{
Whatever \code{.f} returns (typically a named vector, matrix, or
summary), aligned to the graph's vertex order.
}
\description{
\code{gs_compute()} runs any \pkg{igraph} function on the graph carried by a
\code{GraphSpace}, without needing a dedicated \verb{gs_*} wrapper for each one. It
extracts the underlying igraph via \code{\link[igraph:as.igraph]{as.igraph()}}, applies \code{.f}, and returns
the result unchanged. This is the read-only lane onto the whole igraph
ecosystem: measures such as \code{degree()}, \code{betweenness()}, \code{coreness()},
community detection, and distances all work through this one entry point.

It is deliberately \emph{not} a graph-modification path. If \code{.f} returns a graph
(e.g. \code{simplify()}, \code{induced_subgraph()}), \code{gs_compute()} errors, because
reintegrating a modified graph must go through the graph-modification verb
so that node, edge, and coordinate data stay consistent.
}
\examples{
\dontrun{
gs_compute(gs, degree)
gs_compute(gs, "betweenness", directed = FALSE)
gs_compute(gs, cluster_louvain)

## fold a per-vertex result back as a node attribute:
gs$degree <- gs_compute(gs, degree)
}

}
